// export function updateProduct(e, props) {
// e.preventDefault();
// const newTask = {
//   id: props.task.id,
//   title: props.task.title,
//   description: props.task.description,
//   completed: true,
// };
// console.log(newTask);
// const tasksE = [...props.tasks];
// const tasksF = props.tasks.findIndex((task) => task.id === props.task.id);
// tasksE[tasksF] = newTask;
// props.settasks(tasksE);
// console.log("updated index", tasksF, "=>", tasksE);
// props.setshowUpdate(false);
// props.settask({ title: "", description: "" });
// //ref
// props.taskInput?.current?.focus();
// }
